apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-opencargo
  labels:
    app: opencargo
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: opencargo
    release: {{ .Release.Name }}

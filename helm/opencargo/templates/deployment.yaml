apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-opencargo
  labels:
    app: opencargo
    release: {{ .Release.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: opencargo
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: opencargo
        release: {{ .Release.Name }}
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: {{ add .Values.preStop.sleepSeconds .Values.shutdown.endpointDrainSeconds .Values.shutdown.wsCloseGraceSeconds .Values.shutdown.graceSeconds 10 }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: opencargo
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          args:
            - --config
            - /etc/opencargo/config.toml
            - serve
          ports:
            - name: http
              containerPort: 6789
              protocol: TCP
          startupProbe:
            httpGet:
              path: /health/live
              port: http
            periodSeconds: {{ .Values.startup.periodSeconds }}
            failureThreshold: {{ div (add .Values.lease.waitSeconds 60) .Values.startup.periodSeconds }}
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          lifecycle:
            preStop:
              exec:
                command: ["sleep", "{{ .Values.preStop.sleepSeconds }}"]
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /data
            - name: config
              mountPath: /etc/opencargo
              readOnly: true
            - name: tmp
              mountPath: /tmp
          env:
            - name: OPENCARGO_LEASE_WAIT
              value: {{ .Values.lease.waitSeconds | quote }}
            - name: OPENCARGO_ENDPOINT_DRAIN
              value: {{ .Values.shutdown.endpointDrainSeconds | quote }}
            - name: OPENCARGO_SHUTDOWN_GRACE
              value: {{ .Values.shutdown.graceSeconds | quote }}
            - name: OPENCARGO_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-opencargo-secret
                  key: admin-password
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ .Release.Name }}-opencargo-data
        - name: config
          configMap:
            name: {{ .Release.Name }}-opencargo-config
        - name: tmp
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}

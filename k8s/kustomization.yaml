# Compatibility pointer — the manifests moved to base/ when the scaleway/ overlay
# arrived (a kustomization cannot reference its own parent directory).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - base
